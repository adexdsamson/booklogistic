export const GetDeviceWidth = () => {
  // Custom screen sizes
  const lg = 1280;
  const md = 800;
  // This device screen size
  // const deviceWidth = window.innerWidth;
  return [window.innerWidth, md, lg];
}